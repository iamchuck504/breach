{
  "name": "breach",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5200",
    "build": "vite build",
    "preview": "vite preview --port 5200",
    "server": "node server/server.js",
    "check:controller": "node scripts/check-controller.mjs",
    "check:movement": "node scripts/check-movement.mjs",
    "test": "npm run check:controller"
  },
  "dependencies": {
    "lil-gui": "^0.20.0",
    "three": "^0.180.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "vite": "^6.3.5"
  }
}
