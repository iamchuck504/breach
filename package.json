{
  "name": "breach",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5200",
    "build": "vite build",
    "preview": "vite preview --port 5200",
    "server": "node server/server.js",
    "check:controller": "node scripts/check-controller.mjs",
    "check:menu-controller": "node scripts/check-menu-controller.mjs",
    "check:round-flow": "node scripts/check-round-flow.mjs",
    "check:bot-stuck": "node scripts/check-bot-stuck.mjs",
    "check:combat-orientation": "node scripts/check-combat-orientation.mjs",
    "check:online-fire": "node scripts/check-online-fire.mjs",
    "check:cover-fire": "node scripts/check-cover-fire.mjs",
    "check:blindfire": "node scripts/check-blindfire.mjs",
    "check:reticle": "node scripts/check-reticle.mjs",
    "check:impacts": "node scripts/check-impacts.mjs",
    "check:spatial-audio": "node scripts/check-spatial-audio.mjs",
    "check:i18n": "node scripts/check-i18n.mjs",
    "check:shotgun-pattern": "node scripts/check-shotgun-pattern.mjs",
    "check:warmup": "node scripts/check-warmup.mjs",
    "check:movement": "node scripts/check-movement.mjs",
    "check:lobby": "node scripts/check-lobby.mjs",
    "test": "npm run check:controller && npm run check:menu-controller && npm run check:round-flow && npm run check:combat-orientation && npm run check:cover-fire && npm run check:impacts && npm run check:spatial-audio && npm run check:i18n && npm run check:shotgun-pattern && npm run check:warmup && npm run check:lobby && npm run check:online-fire"
  },
  "dependencies": {
    "lil-gui": "^0.20.0",
    "three": "^0.180.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "vite": "^6.3.5"
  }
}
